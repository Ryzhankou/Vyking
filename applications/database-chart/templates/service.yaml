apiVersion: v1
kind: Service
metadata:
  name: {{ include "archer-database.fullname" . }}
  labels:
    {{- include "archer-database.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mysql
      protocol: TCP
      name: mysql
  selector:
    {{- include "archer-database.selectorLabels" . | nindent 4 }}
